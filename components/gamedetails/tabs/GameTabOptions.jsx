import React from 'react';
import GameTabScreenshots from './options/GameTabScreenshots';

const GameTabOptions = ({ id }) => {

  return (
    <GameTabScreenshots  id={id}/>
  )
};

export default GameTabOptions;
